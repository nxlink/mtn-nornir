##########################
# Nornir Runner .env file #
##########################

# Keep .env focused on inventory source configuration only.
# Ports, credentials, workers, platform, timeouts now live in a
# CLI-selectable defaults file (see read.me and --defaults-file).

# Example inventory source variables (not all are used by the script yet):

# Use a SimpleInventory directory (alternative to --inventory-dir)
# INVENTORY_DIR=inventory/

# NetBox inventory (future use)
# NETBOX_URL=https://netbox.example.com
# NETBOX_TOKEN=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Nautobot inventory (future use)
# NAUTOBOT_URL=https://nautobot.example.com
# NAUTOBOT_TOKEN=yyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyy

# CSV inventory (future use)
# INVENTORY_CSV_PATH=inventory/devices.csv
